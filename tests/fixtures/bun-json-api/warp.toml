[package]
name = "bun-json-api"
version = "0.1.0"
description = "Bun JSON API handler fixture for warp pack integration testing"

[build]
lang = "bun"
entry = "src/handler.js"
target = "wasip2"

[runtime]
trigger = "http"
