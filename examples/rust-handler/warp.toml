[package]
name = "rust-handler"
version = "0.1.0"
description = "Minimal Rust async handler example for WarpGrid"

[build]
lang = "rust"
entry = "src/lib.rs"
target = "wasip2"

[runtime]
trigger = "http"
