{
  "name": "t4-ts-http-postgres",
  "version": "0.0.0",
  "private": true,
  "description": "WarpGrid T4 integration test: TypeScript HTTP handler with Postgres via database proxy shim",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "build": "bash scripts/build.sh",
    "build:componentize": "bash scripts/build.sh --componentize-only"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
