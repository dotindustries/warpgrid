package warpgrid:test-handler;

world handler {
  // Standard HTTP handling (WASI 0.2.3)
  import wasi:http/types@0.2.3;

  // WarpGrid database proxy for Postgres connectivity
  import warpgrid:shim/database-proxy@0.1.0;

  // Export the HTTP incoming handler
  export wasi:http/incoming-handler@0.2.3;
}
